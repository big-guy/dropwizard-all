repositories { 
   mavenCentral()
}

apply plugin: 'java'
apply plugin: 'java-library-distribution'

version = '0.7.0'

dependencies {
   runtime "io.dropwizard:dropwizard-core:${project.version}"
}

jar { 
   onlyIf { false }
}
